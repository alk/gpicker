* logging errors to /tmp/fc-errors.log is questionable. Make it
   configurable.
* provide better window title
* add licence preambula for every source file
* running picker without args should display usage
